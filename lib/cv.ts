/**
 * The CV lives at a fixed public path so it can be updated by replacing the
 * file at public/amr-emad-cv.pdf — no code change needed.
 */
export const CV_PATH = "/amr-emad-cv.pdf";

/** The name the file is saved as on the visitor's machine. */
export const CV_FILENAME = "Amr-Emad-CV.pdf";

/** Accessible name, shared by every link that points at the CV. */
export const CV_ARIA_LABEL = "Download Amr Emad's CV (PDF)";
